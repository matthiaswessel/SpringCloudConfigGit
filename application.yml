spring:
    boot:
        admin:
            client:
                url:
                   - http://localhost:8080
    cloud:
        config:
            allowOverride: true
            failFast: true
            overrideNone: true
            overrideSystemProperties: false
management:
    endpoints.web.exposure.include: '*'
    endpoint.health.show-details: always
abc: defmaster1
